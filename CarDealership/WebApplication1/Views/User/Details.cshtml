@using WebApplication1.Data.Models
@model ApplicationUser
@{
    ViewBag.Title = $"{Model.FirstName} {Model.LastName}";
}
<h1>@Model.FirstName @Model.LastName (@Model.Username)</h1>
<h2>Contact information</h2>
<p>Phone number: @Model.PhoneNumber</p>
<p>@(Model.Email == null ? null : $"Email: {Model.Email}")</p>
<h2>Cars</h2>
@if (Model.Cars.Any())
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Photo</th>
                <th>Brand</th>
                <th>Model</th>
                <th>Price</th>
                <th>Details</th>
            </tr>
        </thead>
        <tbody>
            @foreach (Car car in Model.Cars)
            {
                <tr>
                    <td>
                        <img src="@car.ImageUrl" alt="No photo" style="width: 120px; height: 80px; object-fit: cover;" />
                    </td>
                    <th>@car.Brand</th>
                    <th>@car.Model</th>
                    <th>@(car.Price.ToString("#,##0.00", System.Globalization.CultureInfo.InvariantCulture).Replace(",", " ")) €</th>
                    <th>
                        <a class="nav-link text-dark" asp-area="" asp-controller="Car" asp-action="Details" asp-route-id="@car.Id">Details</a>
                    </th>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>This user has no cars</p>
}